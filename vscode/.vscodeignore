# Sources and build intermediates: the shipped code is the esbuild bundle in
# dist/, so nothing under src/ or out/ needs to travel.
src/**
out/**
node_modules/**
.vscode/**
.vscode-test/**
tsconfig.json
eslint.config.mjs
esbuild.mjs
**/*.ts
**/*.map
.gitignore
